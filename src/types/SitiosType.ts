import { EnumType, EnumValues, MapProperty } from "@camberi/firecms";

export type SitiosType = {
  name: string;
  email: string;
  telefono: string;
  address: any;
  googleMapUrl: string;
  website: string;
  tags: [string];
  socialNetwork: [];
};
